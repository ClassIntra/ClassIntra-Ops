# Changelog

本文件记录 ClassIntraOps 的可感知变更。格式参考 Keep a Changelog，版本遵循语义化版本。

## [v0.1] - 2026-09-12

首个公开版本。双形态运维控制台：**launcher**（Avalonia 桌面端，主形态）+ **ops-server**（Node 零依赖 Web 控制台，备用形态）。

### launcher 桌面控制台

- **六页信息架构**：欢迎 / 总览 / 密钥配置 / 跨班对端 / 安装更新 / 服务日志 / 环境设置
- **欢迎页与门禁**：CI 仓库未定位时，欢迎页是唯一落点；密钥 / 跨班 / 日志导航禁用（图标压暗 + tooltip），避免对着空数据猜；定位完成自动进入「配置密钥」
- **总览**：KPI 仪表瓦片（进程在线 / CPU / 内存 / 磁盘，含阈值胶囊与仪表条）、PM2 进程表（状态点、标识徽标、内存 / CPU / 重启列、悬停操作钮）、运行环境与服务摘要右栏、24 槽滚动资源趋势图
- **密钥配置**：schema 由 `server/.env.example` 运行时推导（CI 加字段零漂移）；分组表单 + 密钥打码不回显 + 留空不覆盖 + 保存自动备份；吸底操作栏
- **跨班对端**：RELAY_SERVERS 解析、TCP 握手探活（不误判 relay 的 426 路径）、延迟微型柱、行内探活、终端式探活控制台
- **安装更新**：git 拉取 → 装依赖 → 构建前端 → PM2 启动，实时回显，失败即停不回滚
- **服务日志**：server-out.log / server-error.log 尾部 300 行
- **无边框窗口**：客户区扩展 + 自绘标题栏（拖动 / 双击最大化 / 最小化 / 最大化 / 关闭）+ 右下缩放握把
- **液体流动质感**：低饱和光斑错拍漂移（26s / 32s / 12s）、切页 180ms 淡入、LIVE 呼吸点
- **托盘常驻**：关闭窗口 = 隐藏进系统托盘后台运行（监控定时器持续），左键恢复，右键菜单退出
- **稳定性兜底**：UI 线程未处理异常统一落盘 `logs/crash.log` 并标记 Handled（不闪退）；后台任务未观察异常 SetObserved；高危 async void 路径全部加固
- **崩溃后重建**：密钥 schema 缺 `.env` 时提示并允许创建；PM2 不可用时各页降级提示

### ops-server Web 控制台（备用形态）

- 零第三方依赖，`node server.js` 直跑（默认 `127.0.0.1:9099`）
- 总览 / 环境 / 密钥 / 跨班 / 安装 / 日志，与 launcher 能力对齐

### 备注

- 仅监听 `127.0.0.1`；确需局域网访问 Web 控制台时设置 `OPS_HOST` 并务必同时设置 `OPS_TOKEN`
- 改密钥后必须走「应用配置重启」（`pm2 restart ecosystem.config.js --update-env`），普通 restart 不会重新加载环境变量

[v0.1]: https://github.com/ClassIntra/ClassIntra-Ops/releases/tag/v0.1
